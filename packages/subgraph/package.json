{
  "name": "@mimic-fi/v1-subgraph",
  "private": true,
  "version": "0.0.1",
  "license": "GPL-3.0",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "test": "echo 'No tests for now'",
    "build": "yarn build:mainnet",
    "build:localhost": "NETWORK=localhost ./scripts/build.sh",
    "build:kovan": "NETWORK=kovan ./scripts/build.sh",
    "build:rinkeby": "NETWORK=rinkeby ./scripts/build.sh",
    "build:mainnet": "NETWORK=mainnet ./scripts/build.sh",
    "build:matic": "NETWORK=matic ./scripts/build.sh",
    "deploy:localhost": "NETWORK=localhost ./scripts/deploy.sh",
    "deploy:kovan": "NETWORK=kovan ./scripts/deploy.sh",
    "deploy:rinkeby": "NETWORK=rinkeby ./scripts/deploy.sh",
    "deploy:mainnet": "NETWORK=mainnet ./scripts/deploy.sh",
    "deploy:matic": "NETWORK=matic ./scripts/deploy.sh"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.20.1",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@typescript-eslint/eslint-plugin": "^4.1.1",
    "@typescript-eslint/parser": "^4.1.1",
    "eslint": "^7.9.0"
  }
}
