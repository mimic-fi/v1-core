{
  "name": "@octopus-fi/core-monorepo",
  "private": true,
  "license": "GPL-3.0",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
